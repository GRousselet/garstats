% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/wilcoxmod.R
\name{yuendbt}
\alias{yuendbt}
\title{Bootstrap-t inference for two dependent trimmed means}
\usage{
yuendbt(
  x,
  y,
  tr = 0.2,
  alpha = 0.05,
  nboot = 2000,
  ci.type = c("symmetric", "asymmetric"),
  q.type = 6,
  alternative = c("two.sided", "greater", "less"),
  hyp = 0,
  small.n = TRUE
)
}
\arguments{
\item{x}{Numeric data vector for the first measurement.}

\item{y}{Numeric data vector for the second measurement. Must have the same
length as \code{x}.}

\item{tr}{Trimming proportion in [0, 0.5), with 0.2 default.}

\item{alpha}{Significance level, with 0.05 default.}

\item{nboot}{Number of bootstrap samples, with 2000 default.}

\item{ci.type}{Type of confidence interval for two-sided inference: "symmetric" or
"asymmetric". Ignored for one-sided inference, which always uses signed
bootstrap statistics.}

\item{q.type}{Type of quantile estimator to compute the bootstrap interval.
Default to 6.}

\item{alternative}{Alternative hypothesis: "two.sided", "greater", or "less".}

\item{hyp}{Hypothesis value for the difference in trimmed means. Default to 0.}

\item{small.n}{Default to TRUE to apply the small-sample correction
\code{(tail.count + 1) / (nboot + 1)} to the bootstrap p-value.}
}
\value{
A list containing the confidence interval, trimmed mean estimates,
difference in trimmed means, and p-value.
}
\description{
Computes a bootstrap-t confidence interval and p-value for the difference between the
marginal trimmed means of paired data.
}
\details{
Missing values must be removed before calling this function. When making inferences on means,
the difference between means is the same as the mean of the difference scores. However, this
is not the case for trimmed means, medians and other quantiles. For inferences on the trimmed mean of difference scores,
use \code{trimcibt()} or \code{trimci()} for a non-bootstrap approach.
}
\examples{
set.seed(666)
x <- rnorm(20, mean = 1)
y <- x + rnorm(20)
yuendbt(x, y, ci.type = "symmetric", alternative = "two.sided")
yuendbt(x, y, ci.type = "asymmetric", alternative = "two.sided")
yuendbt(x, y, alternative = "greater")
yuendbt(x, y, alternative = "less")
}
\references{
Wilcox, Rand R. 2022. Introduction to Robust Estimation and Hypothesis Testing. 5th edn.
Statistical Modeling and Decision Science. San Diego, CA: Academic Press.
}
