% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/wilcoxmod.R
\name{pbci}
\alias{pbci}
\title{Percentile bootstrap confidence interval and p-value}
\usage{
pbci(
  x,
  alpha = 0.03,
  hyp = 0,
  alternative = c("two.sided", "greater", "less"),
  q.type = 6,
  small.n = TRUE
)
}
\arguments{
\item{x}{A numeric vector of bootstrap estimates, or a list of results
from \code{\link[=onesampb]{onesampb()}} or \code{\link[=twosampb]{twosampb()}} (or any list containing a numeric
\code{boot.estimates} element).}

\item{alpha}{Significance level, with default to 0.03 (97\% intended coverage).}

\item{hyp}{Hypothesis value, with default to 0.}

\item{alternative}{Alternative hypothesis: "two.sided", "greater", or "less".}

\item{q.type}{Type of quantile estimator. Default to 6.}

\item{small.n}{Logical; apply the small-sample correction
\code{(count + 1) / (nboot + 1)} to the bootstrap p-value.}
}
\value{
A list containing the confidence interval, p-value, hyp,
alternative, and alpha.
}
\description{
Standalone utility to (re)compute a percentile bootstrap confidence
interval and p-value from bootstrap estimates, for any combination of
\code{hyp}, \code{alternative}, and \code{alpha}. This function is used
internally by \code{\link[=onesampb]{onesampb()}} and \code{\link[=twosampb]{twosampb()}}, so it can be applied to the
result list returned by either of those functions without re-running the
bootstrap.
}
\details{
For "greater" the confidence interval is \verb{[lower bound, Inf]}.
For "less" the confidence interval is \verb{[-Inf, upper bound]}.
}
\references{
Wilcox, Rand R. 2022. Introduction to Robust Estimation and Hypothesis Testing. 5th edn.
Statistical Modeling and Decision Science. San Diego, CA: Academic Press.
}
