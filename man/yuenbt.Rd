% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/wilcoxmod.R
\name{yuenbt}
\alias{yuenbt}
\title{Bootstrap-t inference for two independent trimmed means}
\usage{
yuenbt(
  x,
  y,
  tr = 0.2,
  alpha = 0.03,
  nboot = 2000,
  ci.type = c("symmetric", "asymmetric"),
  q.type = 6,
  alternative = c("two.sided", "greater", "less"),
  hyp = 0,
  small.n = TRUE
)
}
\arguments{
\item{x}{Numeric vector for group 1. Missing values must be removed first.}

\item{y}{Numeric vector for group 2. Missing values must be removed first.}

\item{tr}{Trimming proportion in [0, 0.5).}

\item{alpha}{Significance level.}

\item{nboot}{Number of bootstrap samples.}

\item{ci.type}{Type of confidence interval for two-sided inference: "symmetric" or
"asymmetric". Ignored for one-sided inference, which always uses signed
bootstrap statistics.}

\item{q.type}{Type of quantile estimator to compute bootstrap interval. Default to 6.}

\item{alternative}{Alternative hypothesis: "two.sided", "greater", or "less".}

\item{hyp}{Hypothesis value for the difference in trimmed means (default to null).}

\item{small.n}{Logical, default is TRUE to apply the small-sample correction
\code{(tail.count + 1) / (nboot + 1)} to bootstrap p-values.}
}
\value{
A list containing the confidence interval, test statistic,
p-value, estimates, sample sizes, bootstrap estimates, and bootstrap
t-statistics.
}
\description{
Bootstrap-t inference for two independent trimmed means
}
\examples{
set.seed(666)
x <- rnorm(20, mean = 1)
y <- rnorm(20, mean = 0)

yuenbt(x, y, ci.type = "symmetric", alternative = "two.sided")
yuenbt(x, y, ci.type = "asymmetric", alternative = "greater")
yuenbt(x, y, ci.type = "asymmetric", alternative = "less")

}
\references{
Wilcox, Rand R. 2022. Introduction to Robust Estimation and Hypothesis Testing. 5th edn.
Statistical Modeling and Decision Science. San Diego, CA: Academic Press.
}
