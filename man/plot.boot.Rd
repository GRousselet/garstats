% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot.R
\name{plot.boot}
\alias{plot.boot}
\title{Plot a bootstrap distribution}
\usage{
\method{plot}{boot}(
  x,
  ci = NULL,
  ci_level = 0.97,
  alternative = c("two.sided", "less", "greater"),
  estimate = NULL,
  hyp = NULL,
  show_hyp = TRUE,
  label_x_offset = NULL,
  label_y_offset = NULL,
  label_height = 0.1,
  show_ci = TRUE,
  show_estimate = TRUE,
  xlab = NULL,
  ylab = NULL,
  line_colour = "black",
  ci_colour = "orange",
  ci_linewidth = 2,
  estimate_colour = "black",
  label_fill = "orange",
  label_colour = "white",
  label_size = 4,
  one_sided_linetype = "solid",
  hyp_linetype = "dashed",
  theme = NULL,
  ...
)
}
\arguments{
\item{x}{Numeric bootstrap estimates, or a result list containing
\code{boot.estimates}.}

\item{ci}{Optional length-two confidence interval. If \code{x} is a result
list, \code{ci} is taken from \code{x$ci} when available. May include \code{-Inf}
or \code{Inf} for a one-sided interval; its finite bound is drawn as a
full-height dashed vertical line.}

\item{ci_level}{Confidence level used to compute \code{ci} from \code{x} when \code{ci}
is not supplied.}

\item{alternative}{Type of interval when \code{ci} is computed from \code{ci_level}:
\code{"two.sided"} (default), \code{"less"} (upper bound only), or \code{"greater"}
(lower bound only).}

\item{estimate}{Optional point estimate. If \code{x} is a result list,
\code{estimate} is taken from \code{x$estimate} when available.}

\item{hyp}{Optional null-hypothesis value. If \code{x} is a result list,
\code{hyp} is taken from \code{x$hyp} when available.}

\item{show_hyp}{Logical; draw the null-hypothesis reference line when a
finite \code{hyp} value is available.}

\item{label_x_offset}{Horizontal label offset in data units. By default,
a data-dependent offset of 3\% of the bootstrap range is used.}

\item{label_y_offset}{Vertical label offset in density units. By default,
a data-dependent offset of 3\% of the density height is used.}

\item{label_height}{Height of the label position as a fraction of the
density height. Defaults to 0.1.}

\item{show_ci}{Logical; draw the confidence interval when \code{ci} is available.}

\item{show_estimate}{Logical; draw the point estimate when \code{estimate} is
available.}

\item{xlab, ylab}{Axis labels. Default to "Bootstrap estimates" and "Density".}

\item{line_colour, ci_colour, estimate_colour}{Colours for the density,
confidence interval, and estimate.}

\item{ci_linewidth}{Line width for the confidence interval.}

\item{label_fill}{Fill colour used for endpoint labels.}

\item{label_colour}{Text colour used for endpoint labels.}

\item{label_size}{Label text size.}

\item{one_sided_linetype}{Line type for a one-sided confidence bound.}

\item{hyp_linetype}{Line type for the null-hypothesis reference line.}

\item{theme}{A ggplot2 theme. Defaults to \code{garstats::theme_gar()}.}

\item{...}{Additional arguments passed to \code{stats::density()}.}
}
\value{
A ggplot object.
}
\description{
Draws a density curve for a bootstrap distribution exported by the
functions in \code{wilcoxmod.R}. The \code{x} argument may be either the numeric
\code{boot.estimates} vector or the complete result list returned by a bootstrap
function.
}
