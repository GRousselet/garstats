% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/wilcoxmod.R
\name{trimcibt}
\alias{trimcibt}
\title{One-sample bootstrap-t inference for a trimmed mean}
\usage{
trimcibt(
  x,
  tr = 0.2,
  alpha = 0.03,
  nboot = 2000,
  ci.type = c("symmetric", "asymmetric"),
  q.type = 6,
  hyp = 0,
  alternative = c("two.sided", "greater", "less"),
  small.n = TRUE
)
}
\arguments{
\item{x}{Numeric data vector.}

\item{tr}{Trimming proportion in [0, 0.5).}

\item{alpha}{Significance level.}

\item{nboot}{Number of bootstrap samples.}

\item{ci.type}{Type of confidence interval for two-sided inference: "symmetric" or
"asymmetric". Ignored for one-sided inference, which always uses signed
bootstrap statistics.}

\item{q.type}{Type of quantile estimator to compute bootstrap interval. Default to 6.}

\item{hyp}{Hypothesis value for the trimmed mean. Default to the null.}

\item{alternative}{Alternative hypothesis: "two.sided", "greater", or
"less".}

\item{small.n}{Logical; apply the small-sample correction
\code{(tail.count + 1) / (nboot + 1)} to the bootstrap p-value.}
}
\value{
A list containing the trimmed-mean estimate, confidence interval,
test statistic, p-value, sample size, bootstrap estimates, and bootstrap
t-statistics.
}
\description{
Computes a bootstrap percentile-t confidence interval for a trimmed mean.
Missing values are not removed internally. Remove them before calling this
function.
}
\examples{
set.seed(666)
x <- rnorm(20, mean = 1)
trimcibt(x, ci.type = "symmetric")
trimcibt(x, ci.type = "asymmetric")
trimcibt(x, ci.type = "asymmetric", alternative = "greater")
trimcibt(x, ci.type = "asymmetric", alternative = "less")

}
\references{
Wilcox, Rand R. 2022. Introduction to Robust Estimation and Hypothesis Testing. 5th edn.
Statistical Modeling and Decision Science. San Diego, CA: Academic Press.
}
